-- Compatibility facade for `name = "gradient_dracula"` in lazy.nvim, so
-- `opts = {...}` auto-calls require("gradient_dracula").setup(opts).
-- The real implementation lives in the `nvimpire` module.
return require('nvimpire')
